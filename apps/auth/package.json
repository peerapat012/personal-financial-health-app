{
  "name": "personal-financial-health-auth",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.mjs",
    "migrate": "node --env-file=.env provision.mjs migrate",
    "create-owner": "node --env-file=.env --env-file=.env.owner provision.mjs owner",
    "test": "node --test auth.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-auth": "^1.7.4",
    "pg": "^8.23.0"
  }
}
